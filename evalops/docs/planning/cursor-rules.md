# Cursor Rules

- Enforce type-safe APIs and pydantic schemas.
- Require tests for all scoring logic updates.
- Block merges when benchmark regressions exceed threshold.
- Keep ADRs updated with each architecture shift.
