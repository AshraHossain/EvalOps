# Windsurf Rules

- Use phase-tagged branches (`phase-1/*`, `phase-2/*`).
- CI must run evaluation smoke tests before merge.
- Instrument new endpoints with tracing by default.
- Document every metric in the observability catalog.
